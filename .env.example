# NationBuilder Insights (Tableau) API credentials
# Copy this file to `.env` and fill in your own values.
# `.env` is gitignored; this template must only ever hold placeholders.

NB_INSIGHTS_SERVER=https://login.insights.nationbuilder.com
# Set to match the server's REST API version (currently 3.23).
NB_INSIGHTS_API_VERSION=3.23
NB_INSIGHTS_SITE_NAME=your-site-name
NB_INSIGHTS_SITE_URL=your-site-url

NB_INSIGHTS_TOKEN_NAME=your-personal-access-token-name
NB_INSIGHTS_TOKEN_SECRET=your-personal-access-token-secret

# --- Optional: AWS Secrets Manager (for Lambda / production) ---------------
# When set, credentials are read from this secret (a JSON object whose keys are
# the NB_INSIGHTS_* names above) and used only for vars not already set in the
# environment. Leave unset for local runs, which use the values above.
# NB_INSIGHTS_SECRET_ID=nb/insights/prod
# NB_INSIGHTS_SECRET_REGION=us-east-1
# --- Output location -------------------------------------------------------
# Directory for the generated CSV/XLSX files. Defaults to the current directory
# for local runs. In AWS Lambda set this to /tmp (the only writable path).
# OUTPUT_DIR=.

# --- Optional S3 upload ----------------------------------------------------
# When S3_BUCKET is set, every output file is uploaded to
# s3://<bucket>/<prefix>/<filename> and a presigned download URL is printed.
# Leave S3_BUCKET unset to keep output local only.
# Credentials are NOT set here: locally boto3 uses your AWS profile/env, and in
# Lambda it uses the function's IAM execution role.
# S3_BUCKET=your-bucket-name
# S3_PREFIX=zip-state-reports/
# S3_REGION=us-east-1
# Presigned URL lifetime in seconds (default 604800 = 7 days; that is S3's max).
# PRESIGN_EXPIRY_SECONDS=604800
